allowBuilds:
  re2: true
  unrs-resolver: true
onlyBuiltDependencies: ["re2", "esbuild"]
packages:
  # tracked by changesets so renovate config changes get versioned (private,
  # not published) for my own tracking
  - "."
  # packages published to npmjs.org
  - "packages/*"
catalog:
  oxfmt: 0.59.0
