[tools]
# renovate bumps this version automatically. git-cliff stays out of the regular
# pnpm i + cache installation but is still available as a pinned dependency for
# ./.github/workflows/document-release.yml, which reads the version from here.
#
# It can also be used locally to try out changes to the changelog format.
git-cliff = "2.13.1"
